# Assignment Information (these are the *only* things you need to change here between assignments)
set(assignment_name "mp_stickers") # Name of the assignment
set(assignment_version 1.2023.12.0) # Version, where minor=semester_year, patch=semester_end_month, tweak=revision
set(assignment_entrypoints "practice" ) # Entrypoints to run the program