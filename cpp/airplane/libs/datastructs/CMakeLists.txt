add_library(datastructs INTERFACE)
# add_target_sources(datastructs INTERFACE)
target_include_directories(datastructs INTERFACE include)

# add_target_link_libraries(datastructs INTERFACE datastructs)